// Расположения файлов

const curDirPath = process.cwd();

module.exports = {
    EVENTS                                             : `${curDirPath}/events`,

    COMMANDS                                           : `${curDirPath}/commands`,
}